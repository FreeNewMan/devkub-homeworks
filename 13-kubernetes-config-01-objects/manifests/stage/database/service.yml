apiVersion: v1
kind: Service
metadata:
  name: db
  namespace: stage  
  labels:
    app: postgres
spec:
  ports:
  - port: 5432
    name: postgres
    nodePort: 30432
  type: NodePort 
  selector:
    app: postgres